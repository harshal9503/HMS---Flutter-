import 'package:flutter/material.dart';

class OpdIpdSalesScreen extends StatelessWidget {
  const OpdIpdSalesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('OPD/IPD Sales'),
      ),
      body: const Center(
        child: Text('OPD/IPD Sales Screen'),
      ),
    );
  }
}