import 'package:flutter/material.dart';

class BatchExpiryAlertsScreen extends StatelessWidget {
  const BatchExpiryAlertsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Batch/Expiry Alerts'),
      ),
      body: const Center(
        child: Text('Batch/Expiry Alerts Screen'),
      ),
    );
  }
}