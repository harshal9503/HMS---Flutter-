import 'package:flutter/material.dart';

class ClaimInitiationScreen extends StatelessWidget {
  const ClaimInitiationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Claim Initiation'),
      ),
      body: const Center(
        child: Text('Claim Initiation Screen'),
      ),
    );
  }
}